node_modules/
dist/
*.local
.DS_Store

# Corpora the generator reads. Multi-megabyte and separately licensed, so they
# are fetched rather than vendored — run generator/fetch-corpora.sh.
generator/*.dic
generator/*.aff
generator/sv_50k.txt
generator/swe_wordlist

__pycache__/
*.pyc
.pytest_cache/
.venv/
generator/candidates*.json
generator/sweep*.json
generator/sweep*.log
generator/saldo_2.3/
generator/saldo_2.3.tgz

# SAOL material pulled through Karp — Svenska Akademien's data, not ours to
# vendor. python -m kedjan.cli saolpull, or the saolpull workflow, recreates it.
generator/saol-words.txt
generator/karp-cache.json
tsconfig.tsbuildinfo
